import React from "react";

const NameHeader = ({ name }) => (
    <h2
        style={{
            margin: "0",
            textAlign: "center"
        }}
    >
        {name}
    </h2>
);

export default NameHeader;
